public static class SumElements
{
    public static int Sum(double[] vector)
    {
        double sum = 0;
        for (int i = 0; i < vector.lenght, i++)
        {
            sum += vector[i];
        }
        return sum;
    }
}

// сложность алгоритма O(n) - в лучшем случае